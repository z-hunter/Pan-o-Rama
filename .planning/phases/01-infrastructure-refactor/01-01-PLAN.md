---
phase: 01-infrastructure-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  backend/core/config.py,
  backend/core/database.py,
  backend/core/auth.py,
  backend/core/models.py,
  backend/app.py
]
autonomous: true
requirements: [REF-01, REF-02]
must_haves:
  truths:
    - "Application boots with shared core modules."
    - "Database initializes correctly via core/database.py."
  artifacts:
    - path: "backend/core/config.py"
      provides: "Global configuration and constants"
    - path: "backend/core/database.py"
      provides: "DB connection and initialization"
    - path: "backend/core/auth.py"
      provides: "Auth decorators and session management"
    - path: "backend/core/models.py"
      provides: "Common serializers and logic types"
  key_links:
    - from: "backend/app.py"
      to: "backend/core/database.py"
      via: "import and init_db call"
---

<objective>
Initialize the modular backend architecture by extracting core infrastructure from the `app.py` monolith. Establish shared configuration, database management, and authentication utilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Config and Database Core</name>
  <files>backend/core/config.py, backend/core/database.py</files>
  <action>
    - Create `backend/core/__init__.py`.
    - Extract all constants (FOLDER paths, ALLOWED_EXTENSIONS, PLAN_DEFS, etc.) and environment variable lookups from `app.py` into `backend/core/config.py`.
    - Extract `get_db`, `close_db`, `init_db`, and the entire `CREATE TABLE` script into `backend/core/database.py`. 
    - Ensure `init_db` uses the paths defined in `config.py`.
    - Use absolute imports (e.g., `from backend.core.config import DATA_DIR`).
  </action>
  <verify>
    <automated>python -c "from backend.core.database import init_db; print('Database core loaded')"</automated>
  </verify>
  <done>Core config and database modules exist and can be imported.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Auth and Models Core</name>
  <files>backend/core/auth.py, backend/core/models.py</files>
  <action>
    - Extract `hash_token`, `get_current_user`, `require_auth`, `create_session_response` and other auth-related utilities into `backend/core/auth.py`.
    - Extract all `serialize_*` functions and common business logic helpers (e.g., `now_iso`, `safe_float`) into `backend/core/models.py`.
    - Resolve dependencies between these files (e.g., `auth.py` will need `get_db` from `database.py`).
  </action>
  <verify>
    <automated>python -c "from backend.core.auth import require_auth; from backend.core.models import serialize_tour; print('Auth and Models core loaded')"</automated>
  </verify>
  <done>Auth and models core modules exist and can be imported.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor app.py with App Factory</name>
  <files>backend/app.py</files>
  <action>
    - Rewrite `backend/app.py` to implement a `create_app()` factory function.
    - Initialize Flask, CORS, and logging within the factory.
    - Register `teardown_appcontext` and `before_request` / `after_request` hooks using the extracted core modules.
    - Move global route handlers (like `/`, `/login`, etc.) to temporarily remain in `app.py` but ensure they use the new core modules.
    - Ensure `init_db()` is called (or ready to be called) when the app is created.
  </action>
  <verify>
    <automated>python -c "from backend.app import create_app; app = create_app(); print('App factory works')"</automated>
  </verify>
  <done>app.py refactored into a factory pattern and successfully imports core modules.</done>
</task>

</tasks>

<success_criteria>
- The directory `backend/core/` contains functional modules.
- `app.py` is significantly reduced in size and complexity.
- The application can be initialized via the `create_app` factory.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-refactor/01-01-SUMMARY.md`
</output>
