---
phase: 01-infrastructure-refactor
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [
  backend/blueprints/auth.py,
  backend/blueprints/users.py,
  backend/services/email_service.py,
  backend/app.py
]
autonomous: true
requirements: [REF-01]
must_haves:
  truths:
    - "Authentication and User endpoints function via Flask Blueprints."
    - "Email sending logic is abstracted."
  artifacts:
    - path: "backend/blueprints/auth.py"
      provides: "Authentication routes"
    - path: "backend/blueprints/users.py"
      provides: "User account management routes"
    - path: "backend/services/email_service.py"
      provides: "Abstraction for Resend API"
  key_links:
    - from: "backend/app.py"
      to: "backend/blueprints/auth.py"
      via: "app.register_blueprint"
---

<objective>
Refactor authentication and user management logic into separate Blueprints and services to decouple from the main application monolith.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/app.py
@.planning/phases/01-infrastructure-refactor/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Email Service</name>
  <files>backend/services/email_service.py</files>
  <action>
    - Extract `send_resend_email`, `send_verification_email`, `send_password_reset_email`, and `queue_verification_email` into `backend/services/email_service.py`.
    - Create `backend/services/__init__.py`.
    - Ensure it uses constants from `backend.core.config`.
  </action>
  <verify>
    <automated>python -c "from backend.services.email_service import send_verification_email; print('Email service loaded')"</automated>
  </verify>
  <done>Email service module exists and functions.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Auth Blueprint</name>
  <files>backend/blueprints/auth.py</files>
  <action>
    - Create `backend/blueprints/__init__.py`.
    - Create `auth` Blueprint in `backend/blueprints/auth.py`.
    - Move all `/auth/*` routes (`register`, `login`, `verification/resend`, `verify`, `password/forgot`, `password/reset/validate`, `password/reset`, `logout`) to the `auth` Blueprint.
    - Resolve imports using the `backend.core` modules and `backend.services.email_service`.
  </action>
  <verify>
    <automated>python -c "from backend.blueprints.auth import auth; print('Auth blueprint created')"</automated>
  </verify>
  <done>Authentication routes extracted into a Blueprint.</done>
</task>

<task type="auto">
  <name>Task 3: Extract User Blueprint and Register in App</name>
  <files>backend/blueprints/users.py, backend/app.py</files>
  <action>
    - Create `users` Blueprint in `backend/blueprints/users.py`.
    - Move all `/me` routes (`me_get`, `me_patch`, `me_password`) and `get_user_entitlements` (if it's purely a user helper) to the `users` Blueprint.
    - Register both `auth` and `users` Blueprints in the `create_app()` factory in `backend/app.py`.
    - Remove the extracted code from `app.py`.
  </action>
  <verify>
    <automated>python -c "from backend.app import create_app; app = create_app(); print('App factory registered blueprints')"</automated>
  </verify>
  <done>User routes extracted and both Blueprints registered in app.py.</done>
</task>

</tasks>

<success_criteria>
- Auth and user endpoints are functional and isolated in Blueprints.
- `app.py` is further reduced.
- Email logic is centralized in a service.
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-refactor/01-02-SUMMARY.md`
</output>
